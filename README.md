# AI Resume Classifier Web App

An end-to-end Machine Learning web application deployed on Streamlit Cloud that automatically reads resume text and classifies it into its respective professional industry category. Driven by the `snehaanbhawal/resume-dataset`.

## Setup Instructions

### 1. Local Execution
1. Clone the repository to your machine.
2. Install dependencies:
   ```bash
   pip install -r requirements.txt